# SophieBuildsAtHarvard

A bold single-page landing site designed for deployment on Vercel.

## Local preview

Run a simple static server from this directory:

```bash
python3 -m http.server 4173
```

Then open `http://localhost:4173`.

## Deploy

If Vercel CLI is installed and authenticated:

```bash
vercel --prod
```
